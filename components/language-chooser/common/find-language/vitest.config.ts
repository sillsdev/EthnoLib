/// <reference types="vitest" />
import { defineConfig } from "vite";

export default defineConfig({
  // TODO future work: When we upgrade to Vitest version 2.x, uncomment this
  // to require all tests to have assertions. Not supported in Vitest 1.x
  //
  // test: {
  //   expect: {
  //     requireAssertions: true,
  //   },
  // },
});
